fun main (){
    val test = readLine()!!.toInt()
    var array:List<Int> = ArrayList()
    for (i in 0 until test) {
        array = readLine()!!.split(" ").map(String::toInt)
    }
    var res = 0
    array.forEach{
        if (it == 1){
            
        }
    }
}